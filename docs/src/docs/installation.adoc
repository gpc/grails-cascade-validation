[[installation]]
== Installation

The plugin is published to Maven Central.
Add the dependency to your build configuration:

[source,groovy,subs=attributes+]
.build.gradle
----
dependencies {
    implementation 'io.github.gpc:cascade-validation:{version}'
}
----

=== Compatibility

* This version requires Grails {grailsVersion} or later.
* For Grails 5 and 6, use the 4.0.x releases.
