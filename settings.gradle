pluginManagement {
    repositories {
        mavenLocal()
        maven { url = "https://repo.grails.org/grails/core/" }
        gradlePluginPortal()
    }
}

rootProject.name = "cascade-validation"

include 'cascade-validation-example'